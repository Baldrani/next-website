/*
import * as React from "react";
import {Layout} from "../layouts/main";

const Page: React.FC =
    () => (
        <Layout>
            <div>This should be something but I'm not sure yet what exactly</div>
        </Layout>
    )

export default Page
 */