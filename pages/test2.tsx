import * as React from "react";

const Page: React.FC =
    () => (
        <div>
            <p>This should be something but I'm not sure yet what exactly</p>
        </div>
    )

export default Page